# M1 — Closing the loop

**A milestone that closed.** This is history: what was planned, what got built, what was
rejected on the way, and what it cost. It is kept whole because the arguments in it are the
only account of how the code came to be shaped this way.

**It is not the present tense**, and nothing should cite it as though it were. What is in
force is [docs/principles/](../principles/); what exists is *What exists today* in
[roadmap.md](../roadmap.md); why a particular thing was decided is [docs/adr/](../adr/). A
source comment must never point here — see
[P-0093](../principles/0093-a-statement-is-held-true-by-the-thing-it-describes-or-it-is-deleted.md).

Anything this milestone left owed is restated beside its entry in
[roadmap.md](../roadmap.md), because a debt recorded only in history is a debt nobody pays.

---

**Proves:** an LLM can generate constrained IR, we can validate it, compile it to WGSL,
and hot-swap it without dropping a frame.

One Set, hardcoded slots, no UI, synthesized signals only, L1 and L4 only. If this loop
does not close, the whole concept needs rethinking. Everything after this is engineering.

**Closed.** All three clauses hold; what remains unbuilt is tracked in
[roadmap.md](../roadmap.md), not here.

What it cost, since it is the only evidence about how the later estimates should be read:
the loop closed, and then four separate things that had checked clean were found to come
up short at runtime — an unimplemented derivation rule, a Set-composition check that was
specified and written nowhere, a `spawn_rate` requirement no pass enforced, and a clock
that stood still across a frame's substeps. None was found by a test that existed.
Three were found by reading the code against the specification, and the fourth by
disbelieving a comment. Budget for that in every milestone below: the implementation is
not the expensive half.

---

## What the three clauses were proved with

**An LLM can write this language from the specification alone.** Three models were each given
`ir-spec.md`, a one-line aesthetic prompt, and nothing else — no example files, no source, no
tests. Two of the three compiled on the first attempt with no diagnostics at all; the third
failed on one type error and passed on the second attempt after reading it. That is a result
rather than an assumption, at n=3.

What none of them got was the *look* they were asked for. All three rendered a saturated blob,
and the same procedures rendered correctly once exposure and scale were turned down — the code
was right and the viewing conditions were guesses. The specification did not say where the
camera is, what world scale to work in, or that usable exposure falls as element count rises.
It does now, but the underlying problem is that a generator has no way to know a value that
depends on a Set-level dial. That is the tone mapper's job, not the prompt's.

**The hot swap does not drop a frame.** Measured across a swap at capacity 262144, 1280x720,
on a host clock. Two caveats are specific to these numbers: it is a frame *interval* rather
than a GPU cost, and it was taken in the headless test harness, which has no surface and
therefore no vsync and no compositor. Its `device.poll(Wait)` per frame stands in for the
pacing a real window would get, so these are the cost of producing a frame, not of showing one.

| | median | worst |
|---|---|---|
| steady, before the request | 5.0 ms | 13.0 ms |
| the frame the swap landed on | 4.6 ms | — |
| steady, after the swap | 5.0 ms | 10.3 ms |

Three to five frames were rendered between the request going out and the swap landing, which
is what "does not block" means operationally — a blocking receive would make that number zero.
The swap frame has not, across runs, cost more than the worst ordinary frame in the same run;
but it is one sample of a noisy quantity, so the honest claim is "indistinguishable from
jitter", not "free".

What a swap does and does not do is `crates/karakuri-engine/src/swap.rs`'s module
documentation, which states it in force rather than as history.

~2–3 weeks.
