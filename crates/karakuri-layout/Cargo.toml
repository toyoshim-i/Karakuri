[package]
name = "karakuri-layout"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
# Only the tests need a concrete format. The crate itself is format-agnostic:
# it derives `Serialize`/`Deserialize` and lets the caller pick.
serde_json.workspace = true
