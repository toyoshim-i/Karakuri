//! One deck's strip: the residency tally, the trim, the opacity fader,
//! the blend button, the mask mini, and the level meter.

pub mod layout;
pub mod paint;
pub mod state;

pub use layout::*;
pub use paint::*;
pub use state::*;
